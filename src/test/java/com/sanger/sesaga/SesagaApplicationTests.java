package com.sanger.sesaga;

import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class SesagaApplicationTests {
	/*
	 * @Autowired private UserService userService;
	 * 
	 * @Autowired private RoleService roleService;
	 * 
	 * @Test void crearUsuario() { User user = new User("pedro", "password", "pedo",
	 * "capo", "pedrocapo@gmail.com");
	 * 
	 * user.setId(88L); List<Role> roles = new ArrayList<>(); user.setRoles(roles);
	 * User usuario = userService.save(user);
	 * 
	 * assertEquals(usuario.getUserName(), user.getUserName());
	 * 
	 * }
	 * 
	 * @Test void listarUsusarios() { List<User> userList = userService.findAll();
	 * for (User user : userList) { System.out.println(user); }
	 * assertNotNull(userList); }
	 * 
	 * @Test void listarRoles() { List<Role> roleList = roleService.findAll(); for
	 * (Role role : roleList) { System.out.println(role); }
	 * 
	 * assertNotNull(roleList);
	 * 
	 * }
	 */

}
